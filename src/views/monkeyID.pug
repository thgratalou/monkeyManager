html
    head
        style
            include style.css
    body
        include header.pug
        h4 Cette ID correspond au singe suivant :
        p Nom : #{name}
        p Enclos : #{enclosure}
        p ID : #{id}